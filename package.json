{
  "name": "nova",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=core",
    "watch:server": "npm run start:watch --workspace=core",
    "start:server": "npm run start --workspace=core"
  },
  "devDependencies": {
    "prettier": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
